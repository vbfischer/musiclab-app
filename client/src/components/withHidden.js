import type { Element } from 'react';
import type { Breakpoint } from 'material-ui/styles/breakpoints';

type Options = { breakpoint: Breakpoint};


const withHidden = (options: Options = {breakpoint: 'sm'}) => {
    const {breakpoint} = options;

    return
};


export default withHidden;